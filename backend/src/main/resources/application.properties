spring.application.name=api-gateway
server.port=8080

# MongoDB — use environment variable, fallback to localhost
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/gateway_db}
spring.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/gateway_db}

# JWT
jwt.secret=MyS3cur3S3cr3tK3yF0rJWTT0k3nV4l1d4t10n2024
jwt.expiration-ms=3600000

# CORS
cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://smart-api-gateway.vercel.app


# Logging
logging.level.com.portfolio=DEBUG